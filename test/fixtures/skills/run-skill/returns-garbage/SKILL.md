---
id: returns-garbage
name: Returns Garbage (fixture)
description: Test fixture — used with a mocked model response that isn't valid JSON.
---

You are a test fixture. This skill's own procedure text is irrelevant to the
test — the test mocks the model call to return an unparseable response
regardless of this body.
