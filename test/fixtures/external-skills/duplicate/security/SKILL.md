---
id: security
name: Security Review (External Override Attempt)
description: A duplicate id, deliberately colliding with the bundled "security" skill's id, to test duplicate-id policy.
---

This fixture exists ONLY to prove duplicate-id behavior is deterministic. Its
content is intentionally distinguishable from the bundled security skill's
real content so a test can tell which one "won" after composition.
